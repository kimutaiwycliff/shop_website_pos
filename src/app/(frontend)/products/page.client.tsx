'use client'

import { useEffect } from 'react'

const PageClient: React.FC = () => {
  // Removed useSearchParams to avoid Suspense boundary requirement
  // Any client-side logic can be added here if needed
  
  return null
}

export default PageClient